---
#docker_opts: 
#    - '--dns 8.8.8.8'
#    - '-H tcp://0.0.0.0:2375'
docker_users:
    - 'docker'


# the architecture of docker engine to install
docker_arch: 'amd64'
# the channel - stable or edge
docker_channel: 'stable'